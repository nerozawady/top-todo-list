export const Factory = ({name, priority, dueDate, subtodos, element, projectName, projectId}) => ({
  name,
  priority,
  dueDate,
  subtodos,
  element,
  projectName,
  projectId,
});

export const x = 0;
